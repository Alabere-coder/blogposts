import { useState } from 'react';

const useAuthenticateUser = ({ isAuth, setIsAuth }) => {

    // const [isAuth, setIsAuth] = useState(false)


    return (
        <div>AuthenticateUser</div>
    )
}

export default useAuthenticateUser